{
  "name": "smart-customer-management-portal",
  "version": "1.0.0",
  "description": "Smart Customer Management Portal - Fullstack",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && NODE_ENV=production node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
